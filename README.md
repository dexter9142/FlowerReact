Dummy backend that will respond with data from data.json when you will access "/data" endpoint

To run the app please type the following command
```bash
docker compose up -d --build
```
Then the app sould be accessible on
```bash
http://localhost:3000/data
```
